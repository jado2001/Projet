using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Plancher : MonoBehaviour
{

    public bool estSale = false;
    public int propreter = 20;
    public Material matDeBase = GetComponent<MeshRenderer>().material;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        float temps1;
        float temps2;
    }
}
